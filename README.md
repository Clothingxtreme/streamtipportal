# StreamTip Portal

Separate admin portal for settlement review, payout profile change review, user edits, and payout reconciliation.

```bash
cd portal
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if the backend is not running on `http://localhost:5000`.
